FOR DESIGNING:
  The files that has the html files are files named "name_management.php"

DATA AND TABLES:
  Some but most data is stored and loaded within the JS files.
  Look for <td> "data" </td> inside the JS files.

ORGANIZING:
  Create a "css" folder.
  For directory use "../css/name.css"
